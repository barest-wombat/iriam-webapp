# AGENTS.md

## 基本方針

- ユーザーとの会話、ソースコード中のコメント、Markdown、コミットメッセージ、PR 説明文には日本語を使用する。
- 変更はシンプルにし、静的 Web アプリとしての構成を必要なく広げない。
- 推測で進めず、既存ファイルと実際の挙動を確認してから変更する。
- 根本原因を確認し、一時しのぎの修正で済ませない。

## このリポジトリの前提

- ビルド工程や依存管理ファイルは存在しない。依存追加、バンドラー導入、テスト基盤導入は、必要性と影響範囲を確認してから行う。
- アプリ本体は `index.html`、`style.css`、`script.js`、`sw.js`、`manifest.json` で構成される。
- `script.js` は DOM 初期化、状態保存、日付処理、ランク計算、描画が 1 ファイルに集まっている。変更時は責務ごとの影響を分けて確認する。
- 入力状態は `localStorage` の `iriam-state` に保存される。保存形式を変える場合は、既存データへの影響を明示する。
- PWA の静的アセットを変更した場合は、`sw.js` の `CACHE` 更新が必要か確認する。

## 作業手順

- 非自明な変更では、目的、変更方針、検証方法を先に明確にする。
- `tasks/todo.md` が存在する場合は、計画、進捗、レビュー結果をそこに記録する。存在しない場合は、会話内の計画と進捗更新で代替し、必要な場合だけ作成する。
- 実装中に前提が崩れた場合は、立ち止まって方針を更新してから続行する。
- ユーザーから指摘を受けた場合は、再発防止策を検討する。`tasks/lessons.md` が存在する場合は、簡潔に記録する。

## 検証

- 完了前に、変更内容に見合う確認を行う。小さな文書変更ならファイル存在、Markdown 構造、重複、表記を確認する。
- 画面や挙動を変える場合は、静的サーバー経由でブラウザ確認を行う。
- ランク計算を変える場合は、初期状態、ランクアップ、ランクダウン、スキップ使用、月曜スキップパス付与を確認する。
- PWA 関連を変える場合は、キャッシュ名、再読み込み後の反映、マニフェスト、アイコン参照を確認する。
- 検証できなかった場合は、未実施の理由と残るリスクを明示する。

## リファクタリング方針

- まずランク計算を DOM 非依存の純粋関数として切り出し、手動または自動で検証できる安全網を作る。
- 次に、ランク定数、ランク遷移、日付処理、週次スキップパス判定を計算ロジックとして整理する。
- DOM 取得、イベント登録、テーブル描画、カード描画は UI 層としてまとめる。
- `localStorage` の読み書きは保存層に閉じ込め、既存の保存形式は理由なく変えない。
- `style.css` はテーマ変数、フォーム、テーブル、カード、モバイル対応の順に整理する。見た目の刷新はリファクタリングと分けて扱う。
